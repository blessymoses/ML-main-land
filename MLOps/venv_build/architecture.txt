architecture-beta
    group cicd(logos:jenkins)[CICD Jenkins]
    group storage(logos:aws-s3)[S3 Artifacts]
    group query(logos:aws-quicksight)[Discoverability]
    group consumers(cloud)[Consumers]

    service jenkins(logos:jenkins)[Jenkins Pipeline] in cicd
    service venv(logos:python)[venv Build] in cicd
    service depvalidate(logos:prettier)[Dependency Validation] in cicd
    service archive(logos:aws-s3)[Archive to S3] in cicd

    service venvtgz(logos:python)[venv archive] in storage
    service meta(logos:aws-s3)[venv metadata] in storage
    
    service athena(logos:aws-athena)[Athena Table] in query
    service datadog(logos:datadog)[DataDog] in query

    service search(logos:aws-athena)[Search by Package] in consumers
    service dashboard(logos:aws-quicksight)[Dashboard Alerts] in consumers

    jenkins:B -- T:venv
    venv:R -- L:depvalidate
    depvalidate:T -- B:archive

    archive:R -- L:meta
    archive:R -- L:venvtgz

    meta:R -- L:athena
    meta:R -- L:datadog

    athena:R -- L:search
    datadog:R -- L:dashboard